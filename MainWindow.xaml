<Window x:Class="ScreenTranslator2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Screen Translator" Height="700" Width="860"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>

        <SolidColorBrush x:Key="BgDeepBrush"       Color="#0D0F14"/>
        <SolidColorBrush x:Key="BgPanelBrush"      Color="#141720"/>
        <SolidColorBrush x:Key="BgInputBrush"      Color="#10131A"/>
        <SolidColorBrush x:Key="BorderSubtleBrush" Color="#252B3B"/>
        <SolidColorBrush x:Key="AccentBlueBrush"   Color="#4F8EF7"/>
        <SolidColorBrush x:Key="TextPrimaryBrush"  Color="#E8ECF4"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#6B7594"/>
        <SolidColorBrush x:Key="TextMutedBrush"    Color="#3A4160"/>

        <DropShadowEffect x:Key="DropShadow"
                          Color="#000000"
                          BlurRadius="20"
                          ShadowDepth="4"
                          Opacity="0.5"
                          Direction="270"/>

        <DropShadowEffect x:Key="AccentGlow"
                          Color="#4F8EF7"
                          BlurRadius="18"
                          ShadowDepth="0"
                          Opacity="0.55"/>

        <!-- ── Field Label Style ── -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin"     Value="0,0,0,5"/>
        </Style>

        <!-- ── ComboBox Style ── -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background"               Value="{StaticResource BgInputBrush}"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush"              Value="{StaticResource BorderSubtleBrush}"/>
            <Setter Property="BorderThickness"          Value="1"/>
            <Setter Property="FontFamily"               Value="Segoe UI"/>
            <Setter Property="FontSize"                 Value="13"/>
            <Setter Property="Height"                   Value="36"/>
            <Setter Property="Padding"                  Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="10,0,30,0"
                                                      VerticalAlignment="Center"/>
                                    <Path x:Name="Arrow"
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          Fill="{StaticResource TextSecondaryBrush}"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          Margin="0,0,10,0"/>
                                    <ToggleButton x:Name="ToggleButton"
                                                  Focusable="False"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay,
                                                              RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  Background="Transparent"
                                                  BorderThickness="0"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Background="#1C2030"
                                        BorderBrush="{StaticResource BorderSubtleBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="4" Opacity="0.5" Direction="270"/>
                                    </Border.Effect>
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="Padding"    Value="10,6"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#252B3B"/>
                    <Setter Property="Foreground" Value="{StaticResource AccentBlueBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ── TextBox Style ── -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background"      Value="{StaticResource BgInputBrush}"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="CaretBrush"      Value="{StaticResource AccentBlueBrush}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderSubtleBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily"      Value="Cascadia Code, Consolas"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Padding"         Value="12,10"/>
            <Setter Property="SelectionBrush"  Value="{StaticResource AccentBlueBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border CornerRadius="12"
            Background="{StaticResource BgDeepBrush}"
            Effect="{StaticResource DropShadow}"
            Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource BgPanelBrush}"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="18,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="8" Height="8"
                                 Fill="{StaticResource AccentBlueBrush}"
                                 Margin="0,0,10,0"
                                 Effect="{StaticResource AccentGlow}"/>
                        <TextBlock Text="SCREEN TRANSLATOR"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontFamily="Segoe UI"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <Border Background="#1C2030"
                                CornerRadius="4"
                                Padding="6,2"
                                Margin="12,0,0,0"
                                VerticalAlignment="Center">
                            <TextBlock Text="Win+Shift+Q"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontFamily="Cascadia Code, Consolas"
                                       FontSize="10"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="MinimizeButton"
                                Click="MinimizeButton_Click"
                                Width="28" Height="28"
                                Margin="0,0,6,0"
                                ToolTip="Minimize"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bg"
                                            Background="Transparent"
                                            CornerRadius="6"
                                            Width="28" Height="28">
                                        <Path Data="M 6 14 L 22 14"
                                              Stroke="#6B7594"
                                              StrokeThickness="1.5"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#252B3B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="CloseButton"
                                Click="CloseButton_Click"
                                Width="28" Height="28"
                                ToolTip="Close to tray"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bg"
                                            Background="Transparent"
                                            CornerRadius="6"
                                            Width="28" Height="28">
                                        <Path x:Name="CrossPath"
                                              Data="M 8 8 L 20 20 M 20 8 L 8 20"
                                              Stroke="#6B7594"
                                              StrokeThickness="1.5"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bg"        Property="Background" Value="#3B1C24"/>
                                            <Setter TargetName="CrossPath" Property="Stroke"     Value="#F7576A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Rectangle Grid.Row="0"
                       Height="1"
                       Fill="{StaticResource BorderSubtleBrush}"
                       VerticalAlignment="Bottom"/>

            <Grid Grid.Row="1" Margin="20,16,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="160"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button x:Name="SnipButton"
                        Grid.Row="0"
                        Click="SnipButton_Click"
                        Height="44"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg"
                                    Background="#1A2540"
                                    BorderBrush="{StaticResource AccentBlueBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                    <Path Data="M 0 0 L 14 14 M 14 0 L 7 7 M 0 14 L 5 9 M 9 13 C 9 14.7 10.3 16 12 16 C 13.7 16 15 14.7 15 13 C 15 11.3 13.7 10 12 10 C 10.3 10 9 11.3 9 13 Z M -1 3 C -1 4.7 0.3 6 2 6 C 3.7 6 5 4.7 5 3 C 5 1.3 3.7 0 2 0 C 0.3 0 -1 1.3 -1 3 Z"
                                          Fill="{StaticResource AccentBlueBrush}"
                                          Width="16" Height="16"
                                          Stretch="Uniform"
                                          Margin="0,0,10,0"/>
                                    <TextBlock Text="Capture Screen Region"
                                               Foreground="{StaticResource AccentBlueBrush}"
                                               FontFamily="Segoe UI"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#213054"/>
                                    <Setter TargetName="Bg" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#4F8EF7" BlurRadius="16" ShadowDepth="0" Opacity="0.4"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bg" Property="Background" Value="#172238"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Border Grid.Row="1"
                        Margin="0,12,0,0"
                        Background="{StaticResource BgInputBrush}"
                        BorderBrush="{StaticResource BorderSubtleBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <Grid>
                        <TextBlock Text="Captured image will appear here"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontFamily="Segoe UI"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                        <Image x:Name="CapturedImageView"
                               Stretch="Uniform"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="8"/>
                    </Grid>
                </Border>

                <Grid Grid.Row="2" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="FROM" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="SourceLangComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="Detect language"/>
                            <ComboBoxItem Content="English"/>
                            <ComboBoxItem Content="Japanese"/>
                            <ComboBoxItem Content="Russian"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="TO" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="TargetLangComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="English"/>
                            <ComboBoxItem Content="Japanese"/>
                            <ComboBoxItem Content="Russian"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="AI MODEL" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ModelComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="Phi-3 Mini"/>
                            <ComboBoxItem Content="Mistral v0.3"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="OVERLAY" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="OverlayPositionComboBox"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectedIndex="0">
                            <ComboBoxItem Content="Top Left"/>
                            <ComboBoxItem Content="Top Right"/>
                            <ComboBoxItem Content="Bottom Left"/>
                            <ComboBoxItem Content="Bottom Right"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="3" Margin="0,14,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="EXTRACTED TEXT" Style="{StaticResource FieldLabel}"/>
                    <TextBox Grid.Row="1"
                             x:Name="ResultTextBox"
                             Style="{StaticResource ModernTextBox}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="True"
                             Text="Captured text will appear here…"/>
                </Grid>

                <Grid Grid.Row="4" Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="TRANSLATION"
                                   Style="{StaticResource FieldLabel}"
                                   Margin="0,0,8,0"/>
                        <Rectangle Width="28" Height="1"
                                   Fill="{StaticResource AccentBlueBrush}"
                                   VerticalAlignment="Center"
                                   Opacity="0.6"/>
                    </StackPanel>
                    <TextBox Grid.Row="1"
                             Margin="0,5,0,0"
                             x:Name="TranslationBox"
                             Style="{StaticResource ModernTextBox}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             IsReadOnly="True"
                             Text="Translation will appear here…"/>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>
